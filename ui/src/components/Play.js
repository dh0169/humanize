import React from "react";

const Play = () => {
    return (
        // Need to figure out how to standardize this background as the norm for all new pages -> look at old project (greg)
        <div
            style={{
                display: "flex",
                flexDirection: "column",
                alignItems: "center",
                justifyContent: "center",
                minHeight: "100vh",
                gap: "1rem",
                backgroundColor: "#E7FFE4",
            }}
        >

        </div>

        // TODO: Look at + implement chatbox framework recommended by dan 
        // https://docs-shadcn-chat.vercel.app/components/chat-message-list
    )
}

export default Play;